package winter

import org.specs.Specification

class WinterIogiSpecification extends Specification {
  "Winter Iogi integration" should {
    "instantiate a simple object" in {
      skip("waiting for integration test")
    }
  }
}